<div class="jumbotron text-center">

	<% if user_signed_in? %>
<h1><%= "Hola #{current_user.email}"%></h1>
		<% else %>
			<h1><%= "Hola" %></h1>
	<% end %>
 	<p><%= link_to "Crear pregunta", new_question_path %></p>
</div>
  <ul class="container">
  <% @questions.each do |question|%>
<li> <%=  link_to question.title, question %></li>
  <% end %>
</ul>